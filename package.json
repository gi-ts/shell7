{
  "name": "packages-shell",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1",
    "npm-api": "^1.0.0",
    "semver": "^7.3.4"
  },
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "pub": "lerna publish",
    "pub:package": "lerna publish from-package",
    "generate": "gi-ts generate docs.json",
    "configure": "XDG_DATA_DIRS=/usr/share/:/usr/share/gnome-shell/:/usr/lib/mutter-7/ gi-ts config --lock"
  }
}
